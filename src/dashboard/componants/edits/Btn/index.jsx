import { Button } from '@mui/material';
import React from 'react';

function SaveButton(props) {
    return (
        <Button sx={{backgroundColor:"#4880FF"}} variant="contained" size="medium">
        Save
      </Button>
    );
}

export default SaveButton;